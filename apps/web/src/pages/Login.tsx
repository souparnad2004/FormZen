import { LoginForm } from '@/components/login-form'

function Login() {
  return (
    <main className="flex min-h-screen items-center justify-center p-4">
      <LoginForm className="w-full max-w-120" />
    </main>
  )
}

export default Login
